const isRaining = true;

//isRaining = false;

console.log(isRaining);

const person = {
  name : 'Petros',
  age : 34
}

person.age = 35; // we can manipulate properties of an constant object, but not the hole object entity
//person =  {} This wont work
console.log(person);
